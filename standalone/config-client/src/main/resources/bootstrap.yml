management:
  security:
    enabled: false

debug: false

---
spring:
  profiles: default
  cloud:
    config:
      uri: http://localhost:8888

---
spring:
  profiles: cloud
  cloud:
    config:
      uri: http://config-service.apps.local.pcfdev.io

